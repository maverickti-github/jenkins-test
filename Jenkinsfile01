pipeline {
    agent any
    stages {
        stage ('Test') {
            steps {
                sh 'echo Hola Mundo'
                echo 'Build number is $(currentBuild.number)'
            }
        }
    }
}