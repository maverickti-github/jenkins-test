pipeline {
    agent any
    stages {
        stage ('Test') {
            steps {
                sh 'echo Hola Mundo'
                echo "Build number is ${currentBuild.number}"
                echo "Branch name is ${BRANCH_NAME}"
                echo "Job es ${JOB_NAME}"
                echo "El Workspaces is ${WORKSPACE}"
            }
        }
    }
}